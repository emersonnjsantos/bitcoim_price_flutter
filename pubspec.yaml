name: peco_bitcoin
description: A new Flutter project.

# Define a versão do projeto
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Adicionando pacotes necessários
  cupertino_icons: ^1.0.8
  http: ^1.0.0
  logger: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Adicionando a imagem bitcoin.png como asset
  assets:
    - assets/bitcoin.png
    - assets/brazilian-real.png
    - assets/dollar.png
    - assets/euro.png
